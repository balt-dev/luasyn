[package]
name = "luasyn-proc"
version = "0.1.0"
edition = "2024"

[dependencies]
syn = {version = "2", default-features = false, features = ["derive", "proc-macro", "parsing", "printing"]}
quote = "1"

[lib]
proc-macro = true
